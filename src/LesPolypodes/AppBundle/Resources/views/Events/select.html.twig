Select | {{ app.request.get('serverName') }} | {{ calendarName }}

<h3>Formulaire de sélection</h3>

<div class="well">
	{{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
		{{ form_errors(form) }}
	<table>
		<tr><div class="form-group"><td>
		    {{ form_label(form.startDate, "Date et heure de début") }}
		    {{ form_errors(form.startDate) }}</td>

    		<td><div class="col-sm-4">
      		{{ form_widget(form.startDate) }}
    		</div></td>
    	</div></tr>

  		<tr><td><div class="form-group">
      		{{ form_label(form.endDate, "Date et heure de fin") }}
      		{{ form_errors(form.endDate) }}</td>

    		<td><div class="col-sm-4">
      		{{ form_widget(form.endDate) }}
    		</div>
   		</div></td></tr>
   	</table>
   	{{ form_end(form) }}
</div>

{{ include('@LesPolypodesApp/Events/common/menu.html.twig') }}
